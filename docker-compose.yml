version: '3.2'

services:

  ruby_gem:
    image: roar
    build:
      context: .
      dockerfile: mise/containers/ruby_gem/Dockerfile-alpine
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - RUBY_VERSION='2.5'

volumes:
  gem_cache: