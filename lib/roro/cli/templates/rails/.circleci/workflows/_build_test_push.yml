  build-test-push:
    jobs:
      - build
      - test:
          requires: 
            - build
          filters:
            branches:
              only: development
      - push:
          requires: 
            - build
          filters:
            branches:
              only: deploy
