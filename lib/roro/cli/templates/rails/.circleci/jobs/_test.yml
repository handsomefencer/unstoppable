  test:
    <<: *defaults 
    steps:
      - checkout
      - attach_workspace:
          at: /tmp/workspace
      - run: |
          cp -rf /tmp/workspace/roro .
          echo 'source roro/containers/app/ci.env' >> $BASH_ENV
          source $BASH_ENV