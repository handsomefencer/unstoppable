preface: MariaDB Server is one of the most popular open source relational databases. It’s made by the original developers of MySQL and guaranteed to stay open source. It is part of most cloud offerings and the default in most Linux distributions.

env:
  base:
    alpine_db_pkg:
      name: Alpine db package
      value: mariadb
    db_volume:
      name: database volume path
      value: /var/lib/mariadb/data

