

RUN apk add --update \
  build-base \
  file \
  git \
  nodejs \
  tzdata \
  yarn

RUN apk add --update\
  <%= @env[:base][:db_pkg][:value] %>

RUN gem install bundler:<%= @env[:base][:bundler_version][:value] %>

WORKDIR /app