actions:
  - copy_layer('builder')
  - run 'docker-compose build builder-ruby'
  - run 'docker-compose build builder-base'
  - run 'docker-compose build builder-dependencies'
  - run 'DOCKER_BUILDKIT=1 docker build --file mise/containers/app/Dockerfile.builder.generate -t builder/cache --output . .'

