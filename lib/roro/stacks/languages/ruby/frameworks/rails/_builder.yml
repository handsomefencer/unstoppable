actions:
  - copy_layer('builder')
  # - generate_mise
  - run 'docker compose build --with-dependencies builder-dependencies'
  - run 'DOCKER_BUILDKIT=1 docker build --file mise/containers/builder/Dockerfile -t builder/cache --output . .'
