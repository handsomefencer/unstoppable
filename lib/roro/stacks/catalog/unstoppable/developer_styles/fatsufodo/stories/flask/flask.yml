preface: some string

env:
  base:
    app_name:
      name: Name for your app
      value: 'unstoppable app name'

actions:
  - directory 'stage_one', '.', @env
  - run "docker-compose build"
  - run "docker-compose up"
  - run "echo 'You may open your app at http://localhost:5000/'"
