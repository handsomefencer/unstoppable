preface: Django is a high-level Python Web framework that encourages rapid development and clean, pragmatic design. Built by experienced developers, it takes care of much of the hassle of Web development, so you can focus on writing your app without needing to reinvent the wheel. It’s free and open source.

actions:
  - template 'step_1, "./#{@build_vars[:app_name]"
  - system "sudo docker-compose run web django-admin startproject composeexample ."
  - system "cd #{@build_vars[:app_name]} && sudo docker-compose run web django-admin startproject composeexample ."
  - system "sudo chown -R $USER:$USER ."
questions:
  -
    question: What would you like to name your app?
    help: https://hub.docker.com/signup
    action: "@build_vars[:app_name] = answer"

