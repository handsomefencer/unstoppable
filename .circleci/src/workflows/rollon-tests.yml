---
jobs:
- build
- test:
    requires: 
      - build
    filters:
      branches:
        only:
        - rollon-tests
    matrix:
      parameters:
          regex:
              - '**/postgres/**/7_0/**/*'