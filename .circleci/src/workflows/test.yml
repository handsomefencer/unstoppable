---
jobs:
- build
- test-stacks:
    requires: 
      - build
    filters:
      branches:
        only:
        - development
- test-roro:
    requires: 
      - build
    filters:
      branches:
        only:
        - development
       