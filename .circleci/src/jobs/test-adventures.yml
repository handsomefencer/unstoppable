environment: 
  - ANSWERS: "<< parameters.answers >>"
parameters:
  answers:
    type: string
    default: 1\1
  os:
    type: executor
    default: docker-cimg-base
executor: << parameters.os >>


steps:
  - checkout
  - set-variables  
  - artifact-generate
  - artifact-copy
  - artifact-up